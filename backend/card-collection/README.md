Card Collection backend
=======================

Dependency version update check: `mvn versions:display-dependency-updates`. Enforcer plugin will check compatibility.

Same for build plugins: `mvn versions:display-plugin-updates`